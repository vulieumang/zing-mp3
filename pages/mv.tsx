import { MainLayout } from '@components/layout';
import * as React from 'react';

export interface IMvProps {
}

export default function Mv (props: IMvProps) {
  return (
    <div>
      mv
    </div>
  );
}
Mv.Layout = MainLayout;